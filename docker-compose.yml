services:
  morpion:
    build: .
    ports:
      - "127.0.0.1:8081:80"
    restart: always
    volumes:
      - .:/var/www/html
    environment:
      - APACHE_RUN_USER=www-data
      - APACHE_RUN_GROUP=www-data
